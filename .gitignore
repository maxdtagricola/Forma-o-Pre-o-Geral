node_modules
dist
.DS_Store
*.local

# artefatos de build gerados localmente (tsc -b / vite), não fazem parte do código-fonte
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# planilhas de referência/teste com dados reais de cliente — nunca versionar
*.xlsx
